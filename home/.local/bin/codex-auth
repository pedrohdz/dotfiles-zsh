#! /usr/bin/env bash

set -o errexit
set -o pipefail
set -o nounset


printf "\n\033[1;36mBlocking on fetching the API key via password-store…\033[0m"
OPENAI_API_KEY=$(pass show dev-vm/app/codex)
printf "\n"

echo "$OPENAI_API_KEY" | codex login --with-api-key
