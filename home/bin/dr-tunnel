#!/usr/bin/env bash

DELAY=3
TUNNEL_CMD='dr-tunnel'

while true; do
    autossh -M 0 -N $TUNNEL_CMD
    echo '==============================================================================='
    echo "== Sleeping for $DELAY before restarting. (^C to stop.)"
    sleep $DELAY
    echo "== Restarting..."
done
