#!/bin/bash

export basedir=${HOME}/dev/svr/mysql
export datadir=${basedir}-data
export MYSQL_HOME="${datadir}"

echo "Base directory:   ${basedir}"
echo "Data directory:   ${datadir}"
echo "my.cnf home:      ${MYSQL_HOME}"

${basedir}/support-files/mysql.server "$@"
